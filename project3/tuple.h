#pragma once
using namespace std;
#include <string>
#include <vector>
#include <iostream>

class Tuple : public vector<string> {
    /*
    bool operator< (const Tuple & other) const {
    }
    */
    vector <string> values;
};